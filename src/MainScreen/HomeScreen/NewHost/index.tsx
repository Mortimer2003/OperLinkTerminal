import * as React from "react";
import { EditHost } from "../../../Components/EditHost";

// @ts-ignore
export function NewHostPage({ route, navigation }) {

  return <EditHost navigation={navigation} target={null}/>
}
